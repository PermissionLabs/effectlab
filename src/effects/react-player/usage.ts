export const usage = {
  install: "bun add react-player",
  tsx: `// TODO: Add standalone usage example
import { /* Component */ } from "react-player";

export default function Example() {
  return (
    <div>
      {/* TODO: Render the library component */}
    </div>
  );
}`,
};
